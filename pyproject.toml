[project]
name = "research-assistant"
version = "0.1.0"
description = "An AI-powered web app that helps users search, analyze, and query research documents."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
